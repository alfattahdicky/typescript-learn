describe("Data Type", function () {
  it("should primitive must declare", function () {
    let name: string = "Dicky AL Fattah";
    let value: number = 100000;
    let bool: boolean = true;

    console.info(name);
    console.info(value);
    console.info(bool);
  });
});
